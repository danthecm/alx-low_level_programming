#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks for uppercase character
 * Return: 0
 * @c : a placeholder for a character
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
